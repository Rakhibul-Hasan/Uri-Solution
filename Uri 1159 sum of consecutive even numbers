#include<stdio.h>
int main()
{
	int x,s,i;
	scanf("%d",&x);
	if(x==0){ return 0; }
	while(x!=0){ s=0;
	for(i=0;i<10;i++){ if(x%2==0){ s=s+x; }
	x++;
	}
	printf("%d\n",s);
	scanf("%d",&x);
	if(x==0){return 0;}
	 }
	 return 0;
}
